PORT=3000
REDIS_URL=redis://127.0.0.1:6379

# Default token bucket settings (per source)
DEFAULT_CAPACITY=10
DEFAULT_REFILL_RATE=2

# Classifier window (ms) used to compute traffic features
CLASSIFIER_WINDOW_MS=2500
CLASSIFIER_INTERVAL_MS=500
